# Contributing to EternityEngine

1. Fork the repo and clone it locally.
2. Use feature branches (`feature/<topic>`).
3. Run tests before committing.
4. Write clear commit messages.
5. Submit a pull request when ready.
